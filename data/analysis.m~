close all;

calcmpgstats('rt10mpg.txt')

function calcmpgstats(file)

    data = csvread(file);
    print(file)
    print('Gas    - mean=%.3f std=%0.3f',mean(data(:,1)),std(data(:,1)))
    print('Hybrid - mean=%.3f std=%0.3f',mean(data(:,2)),std(data(:,2)))
end